This project fetches user data from the "JSONPlaceholder API" (https://jsonplaceholder.typicode.com/users) 
and saves selected fields (name, username, city, phone, email) into a CSV file. 
The project demonstrates Python basics, working with APIs, lists of dictionaries, CSV files, and modular code using functions.
